#include<bits/stdc++.h>
using namespace std;

// it is unique but not sorted

int main(){
    unordered_set<int>us{2,7,4,9,1,4,5};
    // us.erase(4);
    // cout<<us.count(4)<<endl;
    cout<<us.size();

//all functions work except lower_bound and upper_bound
}